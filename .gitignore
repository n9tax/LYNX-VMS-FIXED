# VMS build artifacts
*.obj
*.olb
*.exe
*.map
*.lis
*.OBJ
*.OLB
*.EXE
*.MAP
*.LIS
Lynx.trace
LYNX.TRACE
BUILD.LOG
build.log

# generated chrtrans headers (makeuctb produces these from the .tbl files)
src/chrtrans/*_uni.h
src/chrtrans/*_kb.h

# transfer archives
*.zip

# VMSINSTAL save sets are build artifacts -- except the released kit,
# which is committed on purpose so people can install without a VAX
# toolchain.  See kit/README.md.
*.A
*.a
!kit/LYNX293.A

# Working notes, not for publication.  HANDOFF.md carries LAN addresses,
# hostnames and account names from the development machines.
HANDOFF.md

# Session guideline files -- never published, per GIT_Rules_for_Claude.txt.
CLAUDE.md
GIT_Rules_for_Claude.txt

# Wiki writeups are scratch artifacts for transfer into the personal wiki,
# not project source.
*.dokuwiki.txt
